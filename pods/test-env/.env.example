# Throwaway LOCAL MOCK credentials + WireGuard keys — NOT real secrets.
# These are SAFE to commit: they only drive the local simulated client environment.
#
#   cp .env.example .env      # then: docker compose -f docker-compose.test.yml up
#
# (The WireGuard keypairs are a matched set generated for this mock; they connect the
#  vpn-sidecar to the mock wg-gateway and nothing else.)

# ── Oracle ─────────────────────────────────────────────────────────────────────
ORACLE_SYS_PASSWORD=oracle_sys_pw     # SYS/SYSTEM (gvenzl ORACLE_PASSWORD)
APP_USER_PASSWORD=presa123            # the PRESA app user's password

# ── SSH jump host ──────────────────────────────────────────────────────────────
SSH_PASSWORD=jumppass123

# ── WireGuard keys (stand in for the FortiGate) ────────────────────────────────
WG_SERVER_PRIVATE_KEY=UEc229e6Zg1LO7TU6ju+oypwkAnQOkYuQr8N/z2R2FY=
WG_SERVER_PUBLIC_KEY=ooty/quJZY3iMDFJryRzkU90FeWrnRSL7MR/nVsGN0k=
WG_CLIENT_PRIVATE_KEY=sEZX//xzwS524bPk5kzYPKWnOqkOGzd2+Y99tWBnq28=
WG_CLIENT_PUBLIC_KEY=WthF0HL6INSU79C6krtVhII5g7VClaobDa/EIgf0DSw=
